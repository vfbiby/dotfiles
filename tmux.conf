# 开启鼠标支持（支持鼠标滚轮滚动查看会话历史、点击切换 panel、拖拽调整大小）
set -g mouse off

# 增加历史回滚行数
#set -g history-limit 50000

# 用 tmux-256color 而不是老的 screen-256color：让 nvim 正确开真彩色
#set -g default-terminal "tmux-256color"
#set -as terminal-overrides ",*:Tc"

# 将状态栏/Tab 栏移动到顶部
set -g status-position top

# 为 macOS 窗口红黄绿三色按钮留出左侧避让空间（避免 Transparent 标题栏下文字重叠）
set -g status-left-length 30
set -g status-left "        [#S] "

# 开启扩展按键支持，保证终端快捷键透传
# 注: 实测 Rio 0.4/0.5 不发送带修饰键的按键(Shift+Enter/Cmd+Delete 均到不了应用),
# 故这些扩展按键配置保持注释、不启用; 换能发修饰键的终端时可再放开
#set -g xterm-keys on
#set -s extended-keys on
#set -as terminal-features 'xterm*:extkeys'
#
#set -sg escape-time 10

# 使用 prefix + h/j/k/l (及大写 H/J/K/L) 移动到 左/下/上/右 panel
bind -r h select-pane -L
bind -r j select-pane -D
bind -r k select-pane -U
bind -r l select-pane -R

bind -r H select-pane -L
bind -r J select-pane -D
bind -r K select-pane -U
bind -r L select-pane -R

# 快捷键 prefix + r 重新加载配置
bind r source-file ~/.tmux.conf \; display-message "Tmux config reloaded!"
